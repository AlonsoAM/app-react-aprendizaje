# URL base de la API de AGROCOM V3 (dev local).
# CORS está abierto en la API, por eso consumimos directo sin proxy.
# Usamos HTTP (no el 7214 HTTPS) para evitar el certificado self-signed en dev.
VITE_API_BASE_URL=http://localhost:5000
